services:
  alpen:
    build: .
    container_name: alpen
    image: alpen
    networks:
      - default
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data/storage

networks:
  default:
    external: true
    name: newt
